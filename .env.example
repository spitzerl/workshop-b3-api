# =============================================================================
# Configuration de l'API Resource Management
# =============================================================================
#
# Ce fichier contient toutes les variables d'environnement nécessaires
# pour faire fonctionner l'API et la base de données.
#
# IMPORTANT : Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins
# Commande : cp .env.example .env (Linux/Mac) ou copy .env.example .env (Windows)
#
# =============================================================================

# -----------------------------------------------------------------------------
# Configuration de la base de données MySQL (pour Docker)
# -----------------------------------------------------------------------------
# Ces variables sont utilisées par le conteneur MySQL
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=resource_management
MYSQL_USER=api_user
MYSQL_PASSWORD=apipassword

# -----------------------------------------------------------------------------
# Configuration des ports
# -----------------------------------------------------------------------------
# Port externe pour accéder à la base de données (localhost:DB_PORT -> MySQL:3306)
DB_PORT=3308

# Port externe pour accéder à l'API (localhost:API_PORT -> API:3001)
API_PORT=3002

# -----------------------------------------------------------------------------
# Configuration de l'application
# -----------------------------------------------------------------------------
# Environnement d'exécution (development, production, test)
NODE_ENV=development

# Exécuter les données de test au démarrage (true/false)
RUN_SEED=false

# -----------------------------------------------------------------------------
# Configuration JWT (Sécurité)
# -----------------------------------------------------------------------------
# IMPORTANT : Changez cette clé en production !
# Générez une clé sécurisée avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-please

# -----------------------------------------------------------------------------
# Configuration pour développement local (SANS Docker)
# -----------------------------------------------------------------------------
# Décommentez et configurez ces variables si vous n'utilisez pas Docker
# et que vous avez une base de données MySQL installée localement

# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=YOUR_MYSQL_PASSWORD
# DB_NAME=resource_management
# PORT=3001

# -----------------------------------------------------------------------------
# Configuration avancée (optionnel)
# -----------------------------------------------------------------------------
# Niveau de logs (error, warn, info, debug)
# LOG_LEVEL=info

# Timeout de connexion à la base de données (en millisecondes)
# DB_CONNECTION_TIMEOUT=60000

# Nombre maximum de connexions simultanées à la base
# DB_CONNECTION_LIMIT=10